package ${packageName}.model.impl;

import ${packageName}.model.${moduleInterface};

/**
 * Description :
 * @author : ${author}
 * @version : ${.now?string("yyyy-MM-dd hh:mm")} 1.0
 */
public class ${moduleImpl} implements ${moduleInterface} {
   
}
