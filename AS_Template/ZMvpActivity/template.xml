<?xml version="1.0"?>
<template
    format="5"
    revision="5"
    name="Z MVP Activity"
    minApi="9"
    minBuildApi="14"
    description="Creates a zaze mvp activity">

    <category value="Activity" />
    <formfactor value="Mobile" />

    <parameter 
        id="className"
        name="Class Name"
        type="string"
        default=""
        help="Class Name" />

    <parameter
        id="activityClass"
        name="Activity Name"
        type="string"
        constraints="class|unique|nonempty"
        suggest="${className}Activity"
        default="Activity"
        help="The name of the activity class to create" />

    <parameter
        id="layoutName"
        name="Layout Name"
        type="string"
        constraints="layout|unique|nonempty"
        suggest="${activityToLayout(activityClass)}"
        default="activity_main"
        help="The name of the layout to create for the activity" />

    <parameter
        id="viewInterface" 
        name="View Interface"
        type="string"
        constraints="class|unique|nonempty"
        suggest="${className}View"
        help="View Interface" />

    <parameter 
        id="presenterInterface"
        name="Presenter Interface"
        type="string"
        constraints="class|unique|nonempty"
        suggest="${className}Presenter"
        help="Presenter Interface" />

    <parameter
        id="presenterImpl"
        name="Presenter"
        type="string"
        constraints="class|unique|nonempty"
        suggest="${className}PresenterImpl"
        help="Presenter" />

    <parameter
        id="generateLayout"
        name="Generate Layout File"
        type="boolean"
        default="true"
        help="If true, a layout file will be generated" />

    <parameter
        id="isLauncher"
        name="Launcher Activity"
        type="boolean"
        default="false"
        help="If true, this activity will have a CATEGORY_LAUNCHER intent filter, making it visible in the launcher" />

    <parameter
        id="backwardsCompatibility"
        name="Backwards Compatibility (AppCompat)"
        type="boolean"
        default="true"
        help="If false, this activity base class will be Activity instead of AppCompatActivity" />
    
    <parameter
        id="packageName"
        name="Package name"
        type="string"
        constraints="package"
        default="com.mycompany.myapp" />

    <!-- 128x128 thumbnails relative to template.xml -->
    <thumbs>
        <!-- default thumbnail is required -->
        <thumb>template_blank_activity.png</thumb>
    </thumbs>

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>
