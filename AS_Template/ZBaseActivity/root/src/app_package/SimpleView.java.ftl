package ${packageName}.view;

import ${corePackageName}.${baseViewName};


/**
 * Description :
 * @author : ${author}
 * @version : ${.now?string("yyyy-MM-dd hh:mm")} 1.0
 */
public interface ${viewInterface} extends ${baseViewName} {
	
}
